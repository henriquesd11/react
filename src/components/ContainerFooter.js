function ContainerFooter({children})
{
    return (
        <div className="container-footer">
            <footer>
                {children}
            </footer>
        </div>
    );
}

export default ContainerFooter;